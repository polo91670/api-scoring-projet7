services:
  - type: web
    name: streamlit-score-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run streamlit_app/app.py --server.enableCORS=false --server.enableXsrfProtection=false
    autoDeploy: true